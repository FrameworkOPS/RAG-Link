# Root railway.toml — Railway uses per-service configs in backend/ and frontend/
# In the Railway dashboard, set Root Directory per service:
#   backend service  → Root Directory: backend
#   frontend service → Root Directory: frontend
